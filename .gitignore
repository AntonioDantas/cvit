# ---- environment / caches ------------------------------------------------
.venv/
__pycache__/
logs/
.DS_Store
*.cache

# ---- downloaded weights (fetched automatically by the scripts) -------------
models/

# ---- large inputs: obtain them from the original sources (README section 2)
datasets/
persons/

# ---- intermediate artefacts regenerated by steps 01-04 ----------------------
backgrounds/

# ---- out/: keep only the produced YOLO dataset -----------------------------
# best.pt (114 MB) is distributed as a release asset (README section 5);
# the figure is tracked as docs/diagram.jpg
out/debug/
out/runs/
out/weight/
out/diagram.png
